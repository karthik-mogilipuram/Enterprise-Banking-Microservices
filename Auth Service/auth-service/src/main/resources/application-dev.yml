spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: postgres
    password: Karthik@007

logging:
  level:
    com.enterprise.banking.auth: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG