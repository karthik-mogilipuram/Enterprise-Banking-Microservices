spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:postgresql://localhost:5432/account_db
    username: postgres
    password: Karthik@007

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.enterprise.banking.account: DEBUG
    org.hibernate.SQL: DEBUG